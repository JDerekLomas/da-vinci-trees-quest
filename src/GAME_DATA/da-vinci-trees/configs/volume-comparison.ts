import { VolumeComparisonInteraction } from '../interactives/interface';

const interaction: VolumeComparisonInteraction = {
  type: 'volume-comparison',
  title: 'Volume Comparison',
  ariaLabel: 'Interactive comparison of cylinder and cone volume models',
};

export default interaction;
