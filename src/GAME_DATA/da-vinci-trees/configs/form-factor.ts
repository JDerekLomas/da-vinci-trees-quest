import { FormFactorInteraction } from '../interactives/interface';

const interaction: FormFactorInteraction = {
  type: 'form-factor',
  title: 'scenes.form_factor.heading',
  ariaLabel: 'Interactive form factor comparison between cylinder, real tree, and cone',
};

export default interaction;
